import { D3TooltipHostDirective } from './d3-tooltip-host.directive';

describe('D3TooltipHostDirective', () => {
  it('should create an instance', () => {
    // const directive = new D3TooltipHostDirective();
    // expect(directive).toBeTruthy();
  });
});
